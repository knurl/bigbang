- add optional support for TLS to the coordinator
- add optional support for TLS between coordinator and workers (experimental)
- add support for Remote Connector
- support PasswordAuthenticator (username/password) and user database
- patch a problem in Starburst helm charts related to TLS load balancers
- automatically kill off pods stuck in Terminating to speed things up
- update to python 3.9.1
- set up local ports differently for AWS, Azure, GCP in case we need to set up
  all three of them for a fancy Remote Connector demo
- only clean up older template instantiations if they are terraform
